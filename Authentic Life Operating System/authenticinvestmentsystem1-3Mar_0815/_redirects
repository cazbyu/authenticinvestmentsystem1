# Expo Router static output — Netlify redirects
# Static HTML files are served directly by Netlify (force=false default).
# This catch-all only fires for routes with no matching static file.

# Serve static assets directly
/_expo/*  /_expo/:splat  200

# SPA fallback for dynamic/unmatched routes only
/*    /index.html   200
